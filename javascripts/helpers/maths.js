/*module is comprised of functions which perform various math actions on their parameters.
Used the multiply by numeral 1 method of converting strings to numbers
used return to produce a final result
Export functions to be called in calculator.js module*/

const add = (num1, num2) => {
return (num1 * 1) + (num2 * 1); 
};
const subtract = (num1, num2) => {
    return (num1 * 1) - (num2 * 1); 
    };
    const multiply = (num1, num2) => {
        return (num1 * 1) * (num2 * 1); 
        };
        const divide = (num1, num2) => {
            return (num1 * 1) / (num2 * 1); 
            };
            export default {add, subtract, multiply, divide};
                        